import Header from "./Header";

const HomePageComponent = () => {
  return (
    <main>
      <Header />
    </main>
  )
}

const Homepage = () => {
  return <HomePageComponent />;
};

export default Homepage;
